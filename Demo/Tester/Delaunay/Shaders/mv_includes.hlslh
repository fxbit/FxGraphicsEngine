
#ifndef H_MV_INCLUDES
#define H_MV_INCLUDES

cbuffer threadParam{
	
// num of the threads
	uint ThreadNum;

// num of the max number of elements in stack
	uint stackMaxSize;
	
// Num of current depth
	uint depth;
	
// Num of thread per row
	uint ThreadNumPerRow;
	
// Num of thread per row
	uint HorizontalThreadNum;
	
	
}

#include "includes.hlslh"

#endif
